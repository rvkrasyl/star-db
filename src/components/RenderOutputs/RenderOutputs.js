import React from "react";

const { 
    Provider : RenderOutputProvider, 
    Consumer : RenderOutputConsumer
} = React.createContext();

export {
    RenderOutputProvider,
    RenderOutputConsumer
}